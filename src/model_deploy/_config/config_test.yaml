imports:
  - config.yaml

parameters:
  daipeproject:
    databricks:
      address: '[Your workspace address]'
      repo:
        base_dir: "/Repos/{repo_name}/{branch_name}"
      job:
        suffix: "{repo_name} ({branch_name})"
